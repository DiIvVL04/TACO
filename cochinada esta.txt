import React, { useEffect, useState } from "react";
import './css/cocinaNavBar.css'
import './css/cocinaPedido.css'

import axios from 'axios';
export const Orden =({Pedido, numMesa}) => {
    console.log(Pedido.cajaBeans);
    let ordenes = Pedido.cajaBeans;

    useEffect(() => {
    }, []);
    
    return(
        <>
            <div  className="container-pedido">
                {ordenes != undefined ? 
                    ordenes.map((orden, i) => (
                <div>
                <div className="num-orden">
                    <span>Orden #{orden.idCaja}</span>
                    <p>Mesa {numMesa}</p>
                </div>
                        <div>
                            <p key={orden.idCaja}> Mesero: {orden.ordenBean.personalBean.nombre}</p>

                            <div className="platillos">
                                
                                <p>{orden.ordenBean.platilloBean.nombre}</p>
                                <p className="precio">{orden.ordenBean.platilloBean.precio}</p>
                            </div>
                        </div>
                    
                </div>
                )) :
                    <p></p>
                    }
            </div>
        </>
    )
}








/*CSS*/

.container-pedido{
    background-color: white;
    min-height: 350px;
    width: 250px;
    height: auto;
    display: flex;
    flex-direction: column;
    padding: 20px;
}

.platillos{
    display: flex;
    justify-content: space-between;
    flex-direction: row;
    margin: 0;
    font-size: 18px;
}

.num-orden{
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-weight: bold;
    font-size: 20px;
    margin-top: 0;
}

.precio{
    font-weight: bold;
}